-------------------------------------------------
${nowTime}: Message ${inOrOut}
reason: ${reason}
Node: ${node}
Connection: ${connection}
Virtual host: ${virtualHost}
Exchange: ${exchangeName}
Routing keys: ${routingKey}
Queue: ${queueName}
Payload:
${message}
-------------------------------------------------